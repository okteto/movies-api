build:
  api:
    context: .

deploy:
  - okteto stack deploy -f mongodb-compose.yml --wait
  - helm upgrade --install movies-api chart --set image=${build.api.registry}/${build.api.repository} --set tag=${build.api.tag}

dependencies:
  movies-frontend:
    repository: git@github.com:okteto/movies-frontend.git
